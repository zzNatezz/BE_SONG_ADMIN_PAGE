const Icon = {
    bell : require('./bell-solid.svg').default,
    envelop : require('./envelope-solid.svg').default,
    lock : require('./Lock.svg').default,
    message : require('./Message.svg').default,
}

export default Icon;